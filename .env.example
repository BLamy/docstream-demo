# --- Frontend (Vite) ---
VITE_AUTH0_DOMAIN=webreplay.us.auth0.com
VITE_AUTH0_CLIENT_ID=
# Optional: set to get a JWT access token instead of an opaque one
VITE_AUTH0_AUDIENCE=

# --- Netlify Functions ---
AUTH0_DOMAIN=webreplay.us.auth0.com
AUTH0_AUDIENCE=
# Set to "true" to run the API without auth (local dev before .env is provisioned)
AUTH_DISABLED=true
